# Cache busting and performance optimization for GitHub Pages
# Note: GitHub Pages doesn't support .htaccess, but this file documents cache strategy

# HTML files should not be cached
<FilesMatch "\.(html|htm)$">
  Header set Cache-Control "no-cache, no-store, must-revalidate, max-age=0"
  Header set Pragma "no-cache"
  Header set Expires "0"
</FilesMatch>

# Assets with hashes can be cached longer (handled by Angular outputHashing)
<FilesMatch "\.(js|css)$">
  Header set Cache-Control "public, max-age=31536000, immutable"
</FilesMatch>

# Images and fonts can be cached
<FilesMatch "\.(jpg|jpeg|png|gif|webp|svg|woff|woff2|ttf|eot)$">
  Header set Cache-Control "public, max-age=31536000"
</FilesMatch>
